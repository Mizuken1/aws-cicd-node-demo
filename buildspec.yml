version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 22
  build:
    commands:
      - npm ci
      - npm test

artifacts:
  files:
    - "**/*"
  exclude-paths:
    - node_modules/**/*
